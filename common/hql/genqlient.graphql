query Deal($uuid: ID!){
    deal(id: $uuid) {
        ID
        ProviderAddress
        PieceCid
        IsVerified
        SignedProposalCid
        InboundFilePath
        ChainDealID
        Checkpoint
        Err
        Message
    }
}